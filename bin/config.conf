server {
       listen 8080;

       server_name mySite;
       root /Users/jac/WebstormProjects/query-statistics-apex/dist;
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}